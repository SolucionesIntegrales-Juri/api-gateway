spring.application.name=api-gateway
server.port=${API_GATEWAY_SERVER_PORT:8090}

# Eureka client default for local dev
eureka.client.service-url.defaultZone=${EUREKA_CLIENT_DEFAULT_ZONE:http://eureka-server:8761/eureka/}

# Add other gateway-specific defaults here if needed (non-secret)